# dictionary features

# keeps oder
# heterogeneous
# values - support duplication
# keys - doesn't support duplication
# in iteration will get only keys as the value of i
# doesn't support indexing
# support nesting
# when we add a key that already includes, it will change the value of that key

# type coversions functions

s={1,2,3,4}
l=list(s)
print(l)
n=set(l)
print(n)